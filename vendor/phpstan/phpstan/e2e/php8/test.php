<?php

namespace RectorPrefix20210504;

function () : void {
    $a = \gmp_random();
    new \DOMImplementationSource();
};
function () : void {
    $ch = \curl_init('https://phpstan.org/');
    if (!\is_resource($ch)) {
        return;
    }
};
